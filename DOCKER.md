## Terminal commands:

docker build --rm -t image-name .

### list images:

docker image ls

### run image:

docker-compose up

### run image in background:

docker compose up -d

# remove image

docker image rm image-name
